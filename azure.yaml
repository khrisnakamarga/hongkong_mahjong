name: hongkong-mahjong
metadata:
  template: azd-init

infra:
  provider: bicep
  path: infra

services:
  web:
    project: .
    language: js
    host: containerapp
    docker:
      path: Dockerfile
      context: .
